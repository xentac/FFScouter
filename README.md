# FF Scouter V2

An attempt to add a build system to FF Scouter V2 to make further development easier.

## Setup

Depends on nodejs and bun. Install dependencies with `bun install`. Do local development with `bun run dev:tm`.
