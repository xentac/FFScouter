# Things to add

- Configure positioning of FF chain button (fixed floating, relative to other elements, etc)
- Add better error messages for when you have a bad key (specifically that the key provided to ff scouter script isn't valid or is not registered)

# Before releasing version 3.0

- Make exposed summary function which, given a timeframe, returns the number of page/hash and which feature found or was ignored for the arrow
  - That way we can use the table to monitor migration progress
- Maybe move the honour bar replacement code to its own feature so that we can track the two separately
- Add note that war monitor has been removed and users should use Torn War Stuff Enhanced to FF Scouter Settings
